[server]
http_bind = "0.0.0.0:8200"
resp3_bind = "0.0.0.0:8201"

[storage]
data_dir = "/data"

# Sentry — authorization policies
[engines.sentry]
enabled = true

# Chronicle — audit events
[engines.chronicle]
enabled = true
